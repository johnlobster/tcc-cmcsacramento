import React from 'react';

// add more props 
// add <Routing-moreProps> to type
// interface Routing-moreProps {
// }

const Routing:React.FunctionComponent = (props) => {
  return(
    <div>
    </div>
  );
}

export default Routing;